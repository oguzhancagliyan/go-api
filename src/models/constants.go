package models

const KeyEmptyErrorMessage = "key shouldn't be empty!"

const KeyNotFoundErrorMessage = "key not found"

const KeyAlreadyExist = "key already exist"

const ModelIsNotValidMessage = "model is not valid"
